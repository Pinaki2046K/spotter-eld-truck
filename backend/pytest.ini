[pytest]
DJANGO_SETTINGS_MODULE = config.settings_test
testpaths = tests
python_files = test_*.py
filterwarnings =
    error
    # collectstatic creates this at deploy time; it does not exist in a fresh checkout.
    ignore:No directory at.*staticfiles:UserWarning
